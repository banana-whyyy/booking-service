# Booking Service
*Дата остановки: 23 июля 2026* 

## Текущий статус (Current state)
- **Сделано:**
    - Часть 1: Авторизация, JWT, работа с refresh токенами
    - Часть 2: CRUD для основных сущностей(комнаты и букинг)
    - Часть 3: Протестированы маршруты для комнат

## Что сделать СРАЗУ по возвращению:
1. Создать роуты для работы с букингом(создание, просмотр и тд)
2. Настроить Celery
3. Развернуть Redis локально
4. Написать первую тестовую фоновую задачу(например, отправка email/уведомления)

## Архитекутрные мысли и контекст
- Celery мы используем для рассылок
- Брокер: Redis
- Для защиты от гонки данных используется пессимистическая блокировка
- commit() и refresh() нужно вызывать в роутах, в crud этого программа не делает

# Как быстро запустить проект
``` 
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Настроить переменные окружения,
# Отредактировать .env указав DATABASE_URL для локальной PostgreSQL
cp .env.example .env

# Применить миграции
alembic upgrade head

# Запустить сервер
uvicorn app.main:app --reload
```

